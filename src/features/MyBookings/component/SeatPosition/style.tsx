import {
    StyleSheet
} from 'react-native'

export const styles = StyleSheet.create({
    header: {
        flexDirection: 'column',
        padding: 10,
        // borderWidth: 1,   
        width: '100%',
    },
})
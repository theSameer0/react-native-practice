import {
    StyleSheet
} from 'react-native'

export const styles = StyleSheet.create({
    success: {
        width: '100%',
        height: '100%',
    },
    image: {
        height: 70,
        alignItems: 'center',
        justifyContent: 'center',
    }
})
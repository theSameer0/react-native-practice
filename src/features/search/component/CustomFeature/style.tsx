import {
    StyleSheet
} from 'react-native'

export const styles = StyleSheet.create({
    body: {
        flexDirection: 'row',
        justifyContent: 'center',
        marginLeft: 20,
    },
    item: {
        flex:1,
        width: 'auto',
        paddingTop: 22,
    }
})